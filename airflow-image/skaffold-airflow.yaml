apiVersion: skaffold/v1beta15
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.IMAGE_NAME}}:1.2.0"
  artifacts:
    - context: .
      image: kai303/tl-airflow
deploy:
  kubectl:
    manifests:
      - deploy-airflow/**
profiles:
  - name: cloudbuild
    build:
      googleCloudBuild: {}